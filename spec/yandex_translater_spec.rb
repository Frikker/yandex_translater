require "spec_helper"

describe YandexTranslater do
  it "has a version number" do
    expect(YandexTranslater::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
